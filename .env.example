# Easy Scholarship — Environment Variables Template
# Copy this file to `.env.local` and fill in your real values.
# `.env.example` is COMMITTED to git; `.env.local` is NOT.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GROQ_API_KEY=
