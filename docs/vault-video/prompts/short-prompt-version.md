# Short Prompt Version

Redo the website around the uploaded vault video. Use the video as the full-screen background and bind the video timeline to scroll progress, so every scroll position matches a frame of the video and the vault feels like an interactive 3D model. Do not autoplay it like a normal background video. Scrub `video.currentTime` using scroll progress.

The final scroll state must show the vault open in the center. Put the Sign Up and Login buttons directly in the middle of the open vault/glow area. When the user clicks either button, trigger a fast zoom-out transition from the vault scene into a polished auth page where the user can enter their information.

Use the Pairing font from the provided font file. If the font file is missing, create the font setup and placeholder folder so I can drop it in later. Keep the design cinematic, premium, dark, futuristic, and realistic. Use the reference images and extracted frames from the ToolBox.

Build the full project structure, not just notes. Include the Next.js/React code, scroll-scrub hook, CSS, auth panel, README, asset paths, and comments showing where to connect real authentication later.
